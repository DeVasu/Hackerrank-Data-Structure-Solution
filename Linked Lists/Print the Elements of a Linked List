/*
  Print elements of a linked list on console 
  head pointer input could be NULL as well for empty list
  Node is defined as 
  struct Node
  {
     int data;
     struct Node *next;
  }
*/
void Print(Node *head)
{    
    while(head!=NULL)         //when last element of list is there
    {
        cout<<head->data<<endl;  //printing the data of the element
        head=head->next;         //next element points to the next element of the list
    }
     
  // This is a "method-only" submission. 
  // You only need to complete this method. 
}
